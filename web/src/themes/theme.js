import { createTheme } from '@mui/material';

export const firstTheme = createTheme({
  palette: {
    primary: { main: '#3A506B' },
  },
  typography: {
    fontSize: 13,
    fontFamily: ['"Helvetica Neue"'],
  },
});
